{
	"name": "cc-ultrawide",
	"version": "1.0.3",
	"ccmodHumanName": "CrossCode Ultrawide",
	"description": "Renders CrossCode at an ultrawide aspect ratio by widening the internal render viewport (genuinely wider field of view, with pixel-perfect integer scaling). Works on desktop and the cc-ios iOS wrapper.",
	"postload": "postload.js",
	"prestart": "prestart.js",
	"ccmodDependencies": {
		"ccloader": ">=2.0.0"
	}
}
